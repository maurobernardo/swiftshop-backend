# Configurações do Backend - SwiftShop
# Copie este arquivo para .env e preencha com seus valores reais

# Porta do servidor (Render/Heroku definem automaticamente via $PORT)
PORT=8888

# Host do servidor
HOST=0.0.0.0

# URL da API (será definida pelo serviço de deploy)
# Exemplo Render: https://swiftshop-backend.onrender.com
API_URL=https://seu-backend.onrender.com

# Banco de dados (SQLite local para desenvolvimento)
# Para produção, considere usar PostgreSQL ou outro banco gerenciado
DATABASE_URL=sqlite:///./swiftshop.db

# Secret Key para JWT (gere uma chave aleatória segura)
SECRET_KEY=sua-chave-secreta-aqui

# Configurações do PayPal (opcional)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_BASE=https://api-m.sandbox.paypal.com

# Configurações de Email (para envio de emails de pedidos)
# Gmail exemplo:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=seu-email@gmail.com
EMAIL_PASSWORD=sua-senha-de-app
EMAIL_FROM=seu-email@gmail.com
EMAIL_FROM_NAME=SwiftShop

